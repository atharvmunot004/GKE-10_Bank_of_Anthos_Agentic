apiVersion: apps/v1
kind: Deployment
metadata:
  name: execute-order-svc
  labels:
    app: execute-order-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: execute-order-svc
  template:
    metadata:
      labels:
        app: execute-order-svc
    spec:
      containers:
      - name: execute-order-svc
        image: execute-order-svc
        ports:
        - containerPort: 8080
        env:
        - name: ASSETS_DB_URI
          value: "postgresql://assets-admin:assets-pwd@assets-db:5432/assets-db"
        - name: PORT
          value: "8080"
        - name: REQUEST_TIMEOUT
          value: "30"
        - name: TIER1
          value: "1000000.0"
        - name: TIER2
          value: "2000000.0"
        - name: TIER3
          value: "500000.0"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: execute-order-svc
  labels:
    app: execute-order-svc
spec:
  selector:
    app: execute-order-svc
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
