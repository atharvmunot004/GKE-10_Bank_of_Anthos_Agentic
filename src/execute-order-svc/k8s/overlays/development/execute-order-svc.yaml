apiVersion: apps/v1
kind: Deployment
metadata:
  name: execute-order-svc
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: execute-order-svc
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
